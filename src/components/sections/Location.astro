---
import images from "../../data/images.yml";

interface Props {
  t: any;
  locale: string;
}
const { t, locale } = Astro.props as Props;
---

<!-- Modern Location & Activities Section -->
<section id="location" class="relative py-32 overflow-hidden">
  <!-- Background Elements -->
  <div
    class="absolute inset-0 bg-gradient-to-br from-forest-50 via-white to-earth-50"
  >
  </div>
  <div
    class="absolute top-0 right-1/4 w-96 h-96 bg-forest-300/10 rounded-full blur-3xl"
  >
  </div>
  <div
    class="absolute bottom-0 left-1/3 w-80 h-80 bg-earth-300/10 rounded-full blur-3xl"
  >
  </div>

  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section Header -->
    <div class="text-center mb-20 animate-on-scroll">
      <h2
        class="text-4xl lg:text-5xl font-serif font-bold text-forest-800 mb-6"
      >
        {t.location.title}
      </h2>
      <p class="text-xl text-sage-600 mb-4">{t.location.subtitle}</p>
      <p class="text-lg text-forest-700 max-w-3xl mx-auto leading-relaxed">
        {t.location.description}
      </p>
    </div>

    <!-- Location Highlight -->
    <div
      class="bg-gradient-to-r from-sage-50 via-white to-forest-50 rounded-2xl p-8 border border-sage-200 shadow-large mb-20 animate-on-scroll"
    >
      <div class="grid lg:grid-cols-2 gap-8 items-center">
        <!-- Location Info -->
        <div>
          <div class="flex items-center space-x-4 mb-6">
            <div
              class="w-16 h-16 bg-forest-gradient rounded-2xl flex items-center justify-center shadow-medium"
            >
              <span class="text-white text-2xl">🗺️</span>
            </div>
            <div>
              <h3 class="text-2xl font-semibold text-forest-800">
                Saint-Léon-sur-Vézère
              </h3>
              <p class="text-sage-600 font-medium">
                {
                  locale === "fr"
                    ? "Au cœur du Périgord Noir"
                    : "Heart of Black Périgord"
                }
              </p>
            </div>
          </div>

          <div class="space-y-4">
            <div class="flex items-center space-x-3">
              <span class="text-sage-600">🚗</span>
              <span class="text-forest-700">
                {
                  locale === "fr"
                    ? "2h30 depuis Bordeaux"
                    : "2h30 from Bordeaux"
                }
              </span>
            </div>
            <div class="flex items-center space-x-3">
              <span class="text-sage-600">✈️</span>
              <span class="text-forest-700">
                {
                  locale === "fr"
                    ? "1h depuis Paris (Bergerac)"
                    : "1h from Paris (Bergerac)"
                }
              </span>
            </div>
            <div class="flex items-center space-x-3">
              <span class="text-sage-600">🏛️</span>
              <span class="text-forest-700">
                {
                  locale === "fr"
                    ? "Patrimoine UNESCO à proximité"
                    : "UNESCO heritage nearby"
                }
              </span>
            </div>
          </div>
        </div>

        <!-- Map Placeholder -->
        <div class="aspect-[4/3] rounded-2xl overflow-hidden shadow-large">
          <img
            src={images.location.map}
            alt={locale === "fr"
              ? "Carte de localisation - Saint-Léon-sur-Vézère"
              : "Location map - Saint-Léon-sur-Vézère"}
            class="w-full h-full object-cover"
          />
        </div>
      </div>
    </div>

    <!-- Activities Showcase -->
    <div class="mb-20">
      <div class="text-center mb-12 animate-on-scroll">
        <h3 class="text-3xl font-serif font-bold text-forest-800 mb-4">
          {
            locale === "fr"
              ? "Activités & Découvertes"
              : "Activities & Discoveries"
          }
        </h3>
        <p class="text-lg text-sage-600 max-w-2xl mx-auto">
          {
            locale === "fr"
              ? "Une région riche en histoire, culture et aventures naturelles"
              : "A region rich in history, culture and natural adventures"
          }
        </p>
      </div>

      <!-- Featured Activities Grid -->
      <div
        class="grid md:grid-cols-2 lg:grid-cols-3 gap-8 mb-12 animate-on-scroll"
      >
        <!-- Cultural Heritage -->
        <div class="group">
          <div
            class="relative bg-gradient-to-br from-white via-sage-50/50 to-transparent rounded-2xl p-6 border border-sage-200 shadow-soft card-hover"
          >
            <div class="absolute -top-4 -right-4">
              <div
                class="w-12 h-12 bg-sage-gradient rounded-xl flex items-center justify-center shadow-sage"
              >
                <span class="text-white text-lg">🏛️</span>
              </div>
            </div>

            <div class="aspect-[4/3] rounded-xl mb-4 overflow-hidden">
              {
                images.location.area[0] && (
                  <img
                    src={images.location.area[0]}
                    alt={
                      locale === "fr"
                        ? "Patrimoine de la région"
                        : "Regional heritage"
                    }
                    class="w-full h-full object-cover"
                  />
                )
              }
            </div>

            <h4
              class="text-xl font-semibold text-forest-800 mb-2 group-hover:text-sage-600 transition-colors"
            >
              {t.location.activities.lascaux}
            </h4>
            <p class="text-forest-600 text-sm">
              {
                locale === "fr"
                  ? "Art pariétal et sites préhistoriques exceptionnels"
                  : "Exceptional cave art and prehistoric sites"
              }
            </p>
          </div>
        </div>

        <!-- Outdoor Adventures -->
        <div class="group">
          <div
            class="relative bg-gradient-to-br from-white via-warm-50/50 to-transparent rounded-2xl p-6 border border-warm-200 shadow-soft card-hover"
          >
            <div class="absolute -top-4 -right-4">
              <div
                class="w-12 h-12 bg-warm-gradient rounded-xl flex items-center justify-center shadow-warm"
              >
                <span class="text-white text-lg">🛶</span>
              </div>
            </div>

            <div class="aspect-[4/3] rounded-xl mb-4 overflow-hidden">
              {
                images.location.area[1] && (
                  <img
                    src={images.location.area[1]}
                    alt={
                      locale === "fr"
                        ? "Activités dans la région"
                        : "Regional activities"
                    }
                    class="w-full h-full object-cover"
                  />
                )
              }
            </div>

            <h4
              class="text-xl font-semibold text-forest-800 mb-2 group-hover:text-warm-600 transition-colors"
            >
              {t.location.activities.canoeing}
            </h4>
            <p class="text-forest-600 text-sm">
              {
                locale === "fr"
                  ? "Descentes en canoë sur la Vézère et randonnées"
                  : "Canoeing on the Vézère river and hiking trails"
              }
            </p>
          </div>
        </div>

        <!-- Castles & Gardens -->
        <div class="group">
          <div
            class="relative bg-gradient-to-br from-white via-forest-50/50 to-transparent rounded-2xl p-6 border border-forest-200 shadow-soft card-hover"
          >
            <div class="absolute -top-4 -right-4">
              <div
                class="w-12 h-12 bg-forest-gradient rounded-xl flex items-center justify-center shadow-medium"
              >
                <span class="text-white text-lg">🏰</span>
              </div>
            </div>

            <div class="aspect-[4/3] rounded-xl mb-4 overflow-hidden">
              {
                images.gallery[3] && (
                  <img
                    src={images.gallery[3].image}
                    alt={
                      locale === "fr"
                        ? images.gallery[3].alt_fr
                        : images.gallery[3].alt_en
                    }
                    class="w-full h-full object-cover"
                  />
                )
              }
            </div>

            <h4
              class="text-xl font-semibold text-forest-800 mb-2 group-hover:text-forest-600 transition-colors"
            >
              {t.location.activities.castles}
            </h4>
            <p class="text-forest-600 text-sm">
              {
                locale === "fr"
                  ? "Châteaux médiévaux et jardins remarquables"
                  : "Medieval castles and remarkable gardens"
              }
            </p>
          </div>
        </div>
      </div>

      <!-- Additional Activities Grid -->
      <div
        class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-5 gap-6 animate-on-scroll"
      >
        <div class="group text-center">
          <div
            class="w-16 h-16 bg-sage-100 rounded-2xl flex items-center justify-center mx-auto mb-3 group-hover:bg-sage-200 group-hover:scale-110 transition-all duration-300"
          >
            <span class="text-sage-600 text-2xl">🦕</span>
          </div>
          <h5 class="font-medium text-forest-800 text-sm">
            {t.location.activities.prehistoric}
          </h5>
        </div>

        <div class="group text-center">
          <div
            class="w-16 h-16 bg-warm-100 rounded-2xl flex items-center justify-center mx-auto mb-3 group-hover:bg-warm-200 group-hover:scale-110 transition-all duration-300"
          >
            <span class="text-warm-600 text-2xl">🌺</span>
          </div>
          <h5 class="font-medium text-forest-800 text-sm">
            {t.location.activities.gardens}
          </h5>
        </div>

        <div class="group text-center">
          <div
            class="w-16 h-16 bg-forest-100 rounded-2xl flex items-center justify-center mx-auto mb-3 group-hover:bg-forest-200 group-hover:scale-110 transition-all duration-300"
          >
            <span class="text-forest-600 text-2xl">🪂</span>
          </div>
          <h5 class="font-medium text-forest-800 text-sm">
            {t.location.activities.paragliding}
          </h5>
        </div>

        <div class="group text-center">
          <div
            class="w-16 h-16 bg-sage-100 rounded-2xl flex items-center justify-center mx-auto mb-3 group-hover:bg-sage-200 group-hover:scale-110 transition-all duration-300"
          >
            <span class="text-sage-600 text-2xl">🧘</span>
          </div>
          <h5 class="font-medium text-forest-800 text-sm">
            {t.location.activities.buddhist}
          </h5>
        </div>

        <div class="group text-center">
          <div
            class="w-16 h-16 bg-warm-100 rounded-2xl flex items-center justify-center mx-auto mb-3 group-hover:bg-warm-200 group-hover:scale-110 transition-all duration-300"
          >
            <span class="text-warm-600 text-2xl">🌿</span>
          </div>
          <h5 class="font-medium text-forest-800 text-sm">
            {locale === "fr" ? "Permaculture" : "Permaculture"}
          </h5>
        </div>
      </div>
    </div>

    <!-- Regional Highlights -->
    <div
      class="bg-gradient-to-r from-forest-50 via-white to-sage-50 rounded-2xl p-8 border border-forest-200 shadow-large animate-on-scroll"
    >
      <div class="text-center mb-8">
        <h3 class="text-2xl font-semibold text-forest-800 mb-2">
          {locale === "fr" ? "À Découvrir Autour" : "Discover Around"}
        </h3>
        <p class="text-sage-600">
          {
            locale === "fr"
              ? "Les incontournables du Périgord"
              : "Must-see attractions in Périgord"
          }
        </p>
      </div>

      <div class="grid md:grid-cols-3 gap-6">
        <div class="text-center">
          <div
            class="w-12 h-12 bg-sage-100 rounded-xl flex items-center justify-center mx-auto mb-3"
          >
            <span class="text-sage-600 text-xl">🍄</span>
          </div>
          <h4 class="font-medium text-forest-800 mb-1">
            {locale === "fr" ? "Gastronomie" : "Gastronomy"}
          </h4>
          <p class="text-sm text-forest-600">
            {
              locale === "fr"
                ? "Truffes, foie gras, noix"
                : "Truffles, foie gras, walnuts"
            }
          </p>
        </div>

        <div class="text-center">
          <div
            class="w-12 h-12 bg-warm-100 rounded-xl flex items-center justify-center mx-auto mb-3"
          >
            <span class="text-warm-600 text-xl">🍷</span>
          </div>
          <h4 class="font-medium text-forest-800 mb-1">
            {locale === "fr" ? "Vignobles" : "Vineyards"}
          </h4>
          <p class="text-sm text-forest-600">
            {
              locale === "fr"
                ? "Bergerac, Monbazillac"
                : "Bergerac, Monbazillac"
            }
          </p>
        </div>

        <div class="text-center">
          <div
            class="w-12 h-12 bg-forest-100 rounded-xl flex items-center justify-center mx-auto mb-3"
          >
            <span class="text-forest-600 text-xl">🏘️</span>
          </div>
          <h4 class="font-medium text-forest-800 mb-1">
            {locale === "fr" ? "Villages" : "Villages"}
          </h4>
          <p class="text-sm text-forest-600">
            {
              locale === "fr"
                ? "Plus beaux villages de France"
                : "Most beautiful villages of France"
            }
          </p>
        </div>
      </div>
    </div>
  </div>
</section>
